# st.bar_chart(horizontal=...) requires Streamlit >= 1.41
streamlit>=1.41
lime>=0.2.0.1
# Pinned: the vectorizer and scaler in model_slim.pkl are pickled sklearn
# objects, which are only guaranteed to load on the version that wrote them.
# Rebuild with build_slim_model.py if you change this pin.
scikit-learn==1.9.0
xgboost>=2.0
nltk>=3.9
pandas>=2.0
numpy>=1.24,<2
scipy>=1.10
